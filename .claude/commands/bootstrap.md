Read `CLAUDE.md` for project conventions, then read `BOOTSTRAP.md` for the full 8-phase bootstrap process.

Before making any changes, ask clarifying questions about the project to fill gaps in your understanding. Then execute all 8 phases from BOOTSTRAP.md in order.

When done, print: project classification, domains created, seed notes created, and validation results.
